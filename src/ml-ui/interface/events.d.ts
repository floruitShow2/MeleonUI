declare namespace MeleonEvents {
  interface ScrollInput {
    detail: {
      deltaX: number
      deltaY: number
      scrollHeight: number
      scrollLeft: number
      scrollTop: number
      scrollWidth: number
    }
  }
}
